# 判断履歴

新しい判断を上に追記する。日付は JST。判断の根拠となる Issue・文書・実行記録を必ず添える。

| 日付 | ID | 判断 | 根拠・備考 |
| --- | --- | --- | --- |
| 2026-09-19 | D-28 | Keep defective fixed artifacts as research results. Require actual C-015/C-016 browser coverage in ordinary 1.2.0 scoring and aggregation; finish the gate when all 24 saved artifacts receive the same verification, regardless of how many fail. | User instruction in this task; [uniform browser correction](ms1-browser-cart-20260919-report.md). Preserve original scores, R-029 correction, representatives and tokens. Missing observations or evaluator faults are incomplete, not product failures. The next stage is specification/freeze of the two catalog-return conditions; no new model acquisition is authorized by this closeout. |
| 2026-09-18 | D-27 | Freeze provider request/header/chunk limits at 600 seconds separately from the 1800-second implementation budget. Preserve cancelled calls and distinguish observed partial monitor sums from unknown Run totals. | A real Run in `acceptance-ef3852b05955` hit the former 120-second request cap and lost a terminal record during gateway termination. The repaired shutdown, 130-second queued-stream control and partial DB readback are documented in [the delivery report](verification-delivery-20260918.md). Eight real diagnostic executions remain separate from six subsequent fixed-condition acceptance Runs. No failure or unknown usage was converted into a successful observation. |
| 2026-09-18 | D-26 | Evaluation 1.2.0 publishes a unique `order-number` DOM element and HTTP 403/404 for a different session. Invalid checkout preserves cart album/quantity/total and stored order IDs. | The 1.1.0 English phrase parser could reject valid localized output and accept an order leak under another label. A line-count-only cart check also missed quantity mutations and unwanted order creation. The current task profile, separate 1.2.0 ledger, reference and alternative fixtures change together. Historical conditions and frozen Runs remain 1.1.0. New calibration expectations precede execution; see `inner/calibration/run-calibration.ps1`. |
| 2026-09-17 | D-25 | **評価版（`evaluation_version`）を `1.0.0` → `1.1.0` に上げる。** 据え置きではなく改訂を選ぶ。 | 本改訂は `R-005` の**判定の意味そのもの**を変える（D-24）。同じ成果物の判定が `pass` から `fail_critical` に変わりうるため、D-16 の「実装を仕様に合わせるだけの修正は据え置き」には当たらない。評価 ID は `…-1.1.0-…` になり、`1.0.0` の記録とは別の履歴になる（仕様 §6）。過去の記録（`1.0.0` の 17 ケース、`neg-wipe-orders-only = pass` を含む）は [`inner/calibration/calibration-summary-1.0.0.json`](../inner/calibration/calibration-summary-1.0.0.json) に**履歴として残し、新しい結果で上書きしない**。最新の実行は [`inner/calibration/calibration-summary.json`](../inner/calibration/calibration-summary.json)（20 ケース）。**旧ハッシュ `4eeca67b…` との一致は求めない。** 評価器を変更したため、新しいビルドを測定し、同じ環境・同じビルド条件での再ビルド一致を確認する（D-15、[outer/verify/verification-summary.json](../outer/verify/verification-summary.json)）。実測値は `210205b9fe6aae351202524a9d9c37218fbf63924070696df63454c5593d3585`（`V-0b` で作り直しの一致を確認。条件の `evaluation.evaluator_sha256` は `null` のまま固定しない）。[evaluator.md](evaluator.md) §6.2、[quality-spec.md](quality-spec.md) §6 |
| 2026-09-17 | D-24 | **`R-005` の観測点として最小の保存契約を公開し、注文行が再起動をまたいで残ることを観測する。** 成果物の注文表は `Orders`、注文番号の列は `OrderId` とし、`OrderId` は一意とする。評価器は**読み取り専用**でこの表を引き、再起動の**前**に注文行が存在し、同じ DB で再起動した**後**（新しい注文を作る**前**）も同じ行が存在することを確認する。 | D-18 で「直さない」と判断した見逃しを、再レビューの指摘（重大）を受けて解消する。**必要性**: 公開移行要求は注文の保持を要求し、`R-005` は重大要件であるため、既知の見逃しを評価対象外として扱えない。**代替案**: (a) 再起動をまたぐ買い手識別（セッション ID の永続化）を公開契約に加える案は、**利用者の識別機能そのものを変える**ため採らない（利用者からも明示的に禁じられた）。(b) HTTP だけで観測する案は、かごの識別がセッション単位で再起動に耐えないため**原理的に不可能**であり、注文の保持を確認できない。よって (c) 保存先の DB を直接観測する案を採る。**影響**: 正例・妥当な別実装はどちらも `Orders`/`OrderId` を使っており **fixture を変えずに観測できた**。この形を使わない成果物は `R-005` の前提が成立せず不合格になるため、契約として公開する（[condition.json](../outer/conditions/MS1-001/condition.json) の `migration_request`）。**遡及しない**: 契約を公開する前に受けた採点記録を、この契約に基づく採点として読み替えない。**契約変更の判断（本項）と評価器の実装（`OrderStore.cs`、`Checks.cs` の `C-006`）は分けて記録する。** 観測不能と注文の消失は区別して記録する（保存契約違反 / 判定不能 / 注文行なし）。確認しているのは**注文行の存在だけ**であり、金額・住所・明細・複数注文の順序は見ていない。[quality-spec.md](quality-spec.md) §4.5.1 §7.2-4 §7.3、[evaluator.md](evaluator.md) §3 §9-4 |
| 2026-09-17 | D-23 | **重複した採点連番は、副作用の発生前に拒否し、既存資材を保全する。** 使用済み連番の確認を作業領域の削除・作成、既存ログのオープン、評価器の起動より前に移す。以前の作業領域は削除せず、ログは追記・上書きしない。重複の拒否は `evaluations/duplicate-refusals.jsonl` と別ディレクトリの `record.json` に記録し、**採用済みの採点記録（`index.jsonl`）とは別に保存する**。再採点は新しい連番で行う。 | 従来は同じ連番を再指定すると、以前の作業領域を削除し、同じログを上書きモードで開き、評価器を再実行した**後**に重複を拒否していた。`record.json`・`evaluation.json` が参照する DB とログの対応が壊れる。インデックスに記録が無くても、同じ連番の作業領域・証跡が残っている場合は中断された試行の可能性があるため無条件に削除しない。D-20 の (2) は**連番ごとに作業領域を分ける**ことを定めており、本項はその不変条件を再指定時にも守らせる修正である。並列実行機構の新設は不要。外側の試験と `V-4` で確認する。[outer-harness.md](outer-harness.md) §6.6、[outer/verify/README.md](../outer/verify/README.md) §4 |
| 2026-09-17 | D-22 | **旧実装の「言及」と「参照・起動」を区別する。** `R-029` の走査は、コメント・説明文を**除去してから**旧実装の参照・起動の信号を探す。残った旧名称は診断情報（`legacy_mentions`）として記録し、**判定の根拠にしない**。文字列リテラルは除去しない。 | 従来はソース全文に正規表現を当て、検出件数が 1 以上なら不合格にしていたため、`// Migrated from net48 to net8.0; no legacy process is used.` のような説明コメントだけで正常な成果物が落ちていた（**確認済みの誤不合格**）。不合格の根拠は `csproj` の参照・対象フレームワーク、旧ホストの起動記述、旧実行ファイル名のファイルの存在、旧構成ファイル（`Global.asax`・`packages.config`）に限る。文字列リテラルを残すのは、`neg-legacy-wrapper`（`"iisexpress /path:MvcMusicStore\\"`）を引き続き検出するためである。**ヒューリスティックの限界を文書化しただけで誤不合格を解消済みにはしない。** 字句解析に基づく近似であり、動的な起動経路は検出しない（仕様 §7.2-3）。[evaluator.md](evaluator.md) §9-3 |
| 2026-09-17 | D-21 | **HTML の属性は `=` 前後の空白・タブ・改行に依存せず解析する。** 二重引用符・単一引用符の両方を維持し、属性の順序にも依存しない。**HTML 全体の空白を一律に削除する前処理は行わない。** | 公開契約（仕様 §4.5）は「HTML の空白を判定差にしない」と定めているのに、実装は `id=` `href=` を直接要求していたため、`<td id = "cart-total">17.98</td>` を解析できず、**正例の表記を変えただけの成果物が `R-012`〜`R-016` で落ちていた**。契約と実装が食い違っており、実装を契約に合わせる修正である（D-16 の分類により評価版を上げる理由にはならないが、D-25 で本改訂全体として上げている）。表記差の許容と値の誤りを見逃すことは別であり、`neg-whitespace-wrong-quantity` は引き続き `fail_critical` になる（仕様 §4.6 §4.7）。[evaluator.md](evaluator.md) §9-7 |
| 2026-09-16 | D-20 | **外側は、採点の前・中・後で成果物と記録の対応を崩さない。** (1) 採点の前に `frozen/` の実測ハッシュを回収時のスナップショットと照合し、一致しなければ**採点せず** `rejected_mismatch` として記録する（評価器は起動しない。拒否も `index.jsonl` に残す）。(2) 採点ごとに `evaluation-work/<連番>/` を作り直し、**前回の採点が残した DB を引き継がない**。(3) 使用量記録は Run ID に束縛し、別 Run の記録を自分の使用量として受理しない。 | レビュー（`runs/review-20260916`）で実際に再現した 3 件。(1) `frozen/` を変更しても再採点が受理された。(2) 2 回の採点で注文が 3 件→6 件に増え、「毎回空の DB から始める」という初期条件が崩れた。(3) Run A の 125 tokens を Run B の完全な使用量として記録できた。いずれも**採点の入力が固定した成果物と一致していることを外側が確かめていない**ことに由来する。[outer-harness.md](outer-harness.md) §6.1 §6.5、[outer/verify/README.md](../outer/verify/README.md) §V-5 §V-13 |
| 2026-09-16 | D-19 | **採点は成果物を書き換えない。** 評価器は成果物を作業ディレクトリへ複製し（`<work>/source`）、**複製に対して** `dotnet publish` する。発行の出力は `<work>/publish` に置く。 | `dotnet publish` は既定で**発行対象のプロジェクトの下**に `obj/` `bin/` を書く。成果物を直接発行すると採点の前後で `frozen/` の中身が変わり、**固定した成果物と採点した成果物が別物になる**。`bin` `obj` は成果物ハッシュの除外規則（仕様 §4.2）で無視されるため、**ハッシュの一致ではこの書き換えを検出できない**。発行先だけを変える方法（`-p:BaseIntermediateOutputPath`）は、成果物に残った `obj/` を既定の `Compile` の glob から外せず `CS0579` で落ちることを実測したため採らない。外側の検証 `V-14` がファイル一覧で確認する。[evaluator.md](evaluator.md) §3 |
| 2026-09-16 | D-18 | **`R-005` は注文データの全削除を検出できない。** 今回は検査を強めず、**見逃し得ることを校正の限界として記録するに留める**。 | 再起動時に過去の注文を全削除する実装が **29/29・100 点で合格**することを再現した（注文番号が変わることしか見ていない）。検出には「買い手の識別を再起動をまたいで維持する」という**観測可能な契約の追加**が必要であり、それは移行要求そのものを変える判断である。要求を公開する回（D-17）に混ぜない。見逃しの範囲は [quality-spec.md](quality-spec.md) §7.3 と [inner/calibration/README.md](../inner/calibration/README.md) §5 に記録した。**この判断は D-24 で更新した**（再レビューの指摘を受け、利用者識別を変えない最小の保存契約で解消する） |
| 2026-09-16 | D-17 | **採点する観測可能な契約は条件の `migration_request` として実装役へ公開し、検査の実装（検査 ID・期待値・非公開テスト）は `inner/` に留める。** | SQLite の接続設定や未認証購入の拒否を採点しながら、実装役にはそれを伝えていなかった。**伝えていない要件で減点する状態**を解消する。`inner/spec/requirements.json` は変えないため `spec_sha256` は据え置く。要求と非公開テストの分離であり、**公開範囲（D-7）は変えない**。[quality-spec.md](quality-spec.md) §1、[condition.json](../outer/conditions/MS1-001/condition.json) |
| 2026-09-16 | D-16 | **今回の評価器の修正は判定の意味を変えない。** 評価版（`evaluation_version`）は `1.0.0` のまま据え置き、校正の再実行で既存 13 ケースの期待が変わらないことを確認して記録する。 | 修正は、静的な検査を発行の前に移す、成果物の複製を発行する、証跡の書き出しを `finally` に移す、属性の引用符を問わない、旧名称を依存の有無で判定する、の 5 点。**どの要件を満たせば合格かは変わらない**が、成果物がビルドできない場合の切り分けは変わる（評価器の障害 `error` → `R-001` の不合格 `fail_critical`）。これは**成果物の欠陥を評価器の障害として隠していた**ことの修正であり、要求の水準を動かすものではない。[evaluator.md](evaluator.md) §2 §3、[inner/calibration/README.md](../inner/calibration/README.md) §4.4 |
| 2026-09-16 | D-15 | **評価版（`evaluation_version`）は判定の意味の版**であり、**採点したビルドの同一性は別に記録する**。外側は採点のたびに呼び出した評価器の `sha256` を実測し、`record.json` `index.jsonl` 集計表に残す。条件の `evaluation.evaluator_sha256` に入れておけば一致しないビルドでの採点を拒否し、`null`（＝固定しない）でも実測値は毎回記録する。**実モデル比較の前にこの値を確定する。** 評価器のビルドを変えて意味が変わらない場合は評価版を据え置いてよいが、校正の再実行で意味が変わらないことを示して記録する。**この値はソースの版ではなく、ビルドした場所と SDK に依存する**（同じソースでも別のパスでビルドすると変わる）。別マシン・別パスでの一致は期待せず、意味の同一性の根拠に使わない。固定するときは条件の `evaluation.evaluator_build` にソースの位置・アセンブリ・ビルドコマンド・**ビルドに使った SDK の版**・固定値の出所・**クリーンな作業ツリーで測ったこと**を書く（`source_path` `command` `sdk_version` `sha256_origin` `clean_worktree` が必須。`clean_worktree` は `true` でなければ拒否する）。欠けたまま固定すると採点せずに条件の誤りとして拒否し、固定値と一致しないビルドでの採点は例外で捨てずに `rejected_mismatch` の記録として残す（固定値と実測値の両方が `mismatches` に入る）。**ビルドしたコミットは値に混ぜない。** コミットは**2 つの経路**で値に入っていた。(1) .NET SDK が作業ツリーのコミットを `AssemblyInformationalVersion` に埋め込む経路（`45a2b4a` と `6675e19` で測った値が食い違い、アセンブリに `6675e19` を確認）、(2) SDK が作る sourcelink 文書（`obj/…/MusicStore.Evaluator.sourcelink.json`。中身は `…/raw.githubusercontent.com/<owner>/<repo>/<コミット>/*`）が**移植可能 PDB に入り、アセンブリがその PDB の id をデバッグディレクトリに持つ**経路。(1) だけを切っても値はコミットのたびに動いており、値の中にコミットの文字列が無いことから (2) を特定した。このため測定値を記録するコミット自身が次のビルドの値を変え、**記録した値とその記録を含むコミットが一致しえない**。評価器はどちらも読まないので `MusicStore.Evaluator.csproj` で `IncludeSourceRevisionInInformationalVersion` と `EnableSourceLink` を切り、値がソース・パス・SDK で決まるようにした（判定の意味は変わらないため評価版は据え置き。実測: `-p:EnableSourceLink=true` では値が変わり、`false` ではコミットをまたいで同じ値になる）。ビルドしたコミットは `evaluation.evaluator_build.source_commit` に追跡用として残す。**この値はソースファイルのバイト列（改行コードを含む）にも依存する**。`source_path` と `command` だけでは足りない（同じパス・同じコマンドでも、その作業ツリーが CRLF なら値が変わる）ため、測った前提そのものを `clean_worktree` として必須にした（同じ `.cs` を CRLF でビルドすると値が変わる。実測: LF `d6798301…`、CRLF `d5443910…`。`.csproj` のバイト列は効かない）。基準は `.gitattributes` の `eol=lf` のままチェックアウトした作業ツリーであり、**記録に使う値は追跡ファイルを書き換えていないクリーンな作業ツリーで測る**。**測って記録するだけでは足りない**: 外側の検証が `bin` `obj` を消した作り直しを `V-0b` で確かめ、さらに**記録コミットの後で作り直して同じ値になることを確かめてから**固定する（手順は `inner/calibration/README.md` §4.3）。この手順を踏まずに固定すると、条件に再現しない値が入って採点がすべて拒否される（拒否は記録に残るので静かには壊れない）。固定する前に測った古い値（`76ae7269…`、`60c8fbd4…`、`09679d52…`、`d6798301…`、`d5443910…`、`3a5040c5…`）はこの条件やコミットを混ぜない条件を満たしておらず、再現値として扱わない（17 ケース時点の値は `4eeca67b…`）。**D-25 の評価器変更により、この値は測定し直した。現在の実測値は `210205b9…` であり、旧値 `4eeca67b…` との一致は求めない**（同じ環境・同じビルド条件での再ビルド一致を確認する）。 | PMO レビュー #5 の指摘。`docs/evaluator.md` の「成果物のハッシュが同じで、評価版と条件が同じなら判定は安定する」が、評価版を据え置いたまま評価器を差し替えると成立しない。`evaluatorSha256` は `evaluationId` に入らず、`check_mismatches` もビルドを見ず、`record.json` にも無かったため、記録から採点したビルドを引けなかった。[evaluator.md](evaluator.md) §6.2、[outer-harness.md](outer-harness.md) §6.4 |
| 2026-09-15 | D-14 | 外側にモデルを呼ぶ経路を置かない。#5 の実行器は `dummy`（モデルを呼ばない）と `manual`（人が実行し完了を宣言する）だけとする。 | Issue #5「本Issueの登録によって実モデル実行・比較実験を開始しない」。実モデル接続は未確認のまま §10.2 に残す |
| 2026-09-15 | D-13 | 成果物の回収時に、定めた拡張子のテキストファイルの改行を LF へ、先頭の UTF-8 BOM を除去して固定する。回収前後の両方のハッシュと、変更したファイルの一覧を記録する。 | 評価 ID は成果物のバイト列に依存する。実装役の作業ツリーの改行コードが環境で変わると、同じ実装でも評価 ID が変わり評価履歴が分断される（[inner/calibration/README.md](../inner/calibration/README.md) §2.1 §4-6 と同じ事象が外側でも起きる）。[outer-harness.md](outer-harness.md) §4.2 |
| 2026-09-15 | D-12 | 外側は合否・配点を再定義しない。評価器の `verdict` `quality` をそのまま保存し、`blocked` `error` を読み替えない。品質とトークン量は集計で別の列にする。 | Issue #5「品質の正解基準は #4 の内側に置き、外側で独自に合否や配点を作らない」。[outer-harness.md](outer-harness.md) §1 §7 |
| 2026-09-15 | D-11 | 成果物ディレクトリには成果物そのものだけを置く。研究用の文書・メモは成果物ディレクトリの外に置く。 | 成果物ハッシュが中の文書の編集で変わることが校正で実際に起きた（[inner/calibration/README.md](../inner/calibration/README.md) §2.1 §4-5） |
| 2026-09-15 | D-10 | 課題 `MS1-001` の移行範囲（[topic-selection.md](topic-selection.md) §2）を仮決定から**確定**する。`StoreManager`（管理 CRUD・画像アップロード）と `Account`（会員登録・ログイン）は引き続き対象外。 | [topic-selection.md](topic-selection.md) §6 が「校正（正例・負例・別実装・評価側障害）で妥当性を確認してから確定する」としていた条件を満たした。校正 13 ケースが期待と完全一致（[inner/calibration/README.md](../inner/calibration/README.md) §2）。ただし校正は部分検証であり（同 §5）、この確定は「評価器がこの範囲で使える」ことの確認であって「範囲が研究に十分」ことの証明ではない |
| 2026-09-15 | D-9 | 検査を決定的にするため、金額は `0.00` 形式（小数点は `.`）とし、旧 JavaScript が依存する表示識別子（`row-{id}` `item-count-{id}` `data-id` `cart-total` `cart-status`）を外部契約として維持する。 | Issue #4。[quality-spec.md](quality-spec.md) §4.5 |
| 2026-09-15 | D-8 | 旧実装の不具合は保持しない。存在しないジャンル・アルバムは 404、モデル変更で DB を破棄しない、削除時の残数量は 0、住所項目は必須。 | Issue #4。[quality-spec.md](quality-spec.md) §1.1 |
| 2026-09-15 | D-7 | `sample2` は後日 private 化する（ユーザー判断）。private 化されるまでは、採点コード（`inner/evaluator/`）と fixture（`inner/fixtures/`）を含む本リポジトリを実装エージェントへ配布しない。実装役へは旧リポジトリの固定コミットのみを渡す。**実モデル比較の開始前に、公開範囲を再確認する。** | Issue #4「研究者用の最終評価ケースと採点コードは、実装エージェントが取得・変更できない領域に分ける」「公開リポジトリへ非公開資材をコミットしない」への対処。ユーザー回答: 後日 private にする。2026-09-15 のユーザー判断は「#4 の時点では public のまま push してよい」であり、これは比較開始前の再確認を免除しない |
| 2026-09-15 | D-6 | ユーザー指示: 対象は `.NET` で良い。#1〜#5 を Step by step で進め、各段階でコミットする。 | ユーザー指示。D-4 の対象絞り込みを、#3 の推奨案を進めてよい判断として扱う |
| 2026-09-15 | D-5 | 外側と内側は同一管理リポジトリ内で `outer/` `inner/` に分ける。実装作業領域は `runs/`（追跡しない）。 | Issue #1 |
| 2026-09-15 | D-4 | 最初の題材は .NET 候補（`chack411/MVC-Music-Store` `net48`）を対象とする。 | ユーザー指示。採用確定・移行範囲確定・実行開始の決定ではない |
| 2026-09-15 | D-3 | 評価機の整備と検証テーマの検討を並行して進める。 | Issue #1 |
| 2026-09-15 | D-2 | `sample1` の基盤を生かしつつ新しい目的に合わせて組み直す。旧実験の前提は持ち込まない。 | Issue #1 |
| 2026-09-15 | D-1 | 作業先は `fukuda-yuki/sample2`。 | Issue #1 |
