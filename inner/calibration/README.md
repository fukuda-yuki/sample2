# 評価器の校正（`inner/calibration`）

[品質評価仕様 §7.1](../../docs/quality-spec.md) が求める 4 分類の校正を実行し、その結果を記録する。

- 実行: [`run-calibration.ps1`](run-calibration.ps1)
- 生の記録: [`calibration-summary.json`](calibration-summary.json)（実行時に生成される実際の出力）
- 過去の記録: [`calibration-summary-1.0.0.json`](calibration-summary-1.0.0.json)（評価版 `1.0.0` の 17 ケース。履歴として保持し、上書きしない）
- 評価器の契約: [docs/evaluator.md](../../docs/evaluator.md)
- **モデルは一切呼び出さない。** 成果物を固定し、評価器だけを検証する。

```powershell
pwsh -NoProfile -File .\inner\calibration\run-calibration.ps1
```

**実行するホストを記録に合わせる。** 記録した [`calibration-summary.json`](calibration-summary.json) は
**PowerShell 7**（本記録では `pwsh` 7.6.6）で実行した出力である。
Windows PowerShell 5.1 で実行すると、評価側の障害 2 件の `stderr` の書き方だけが変わる（§5-15）。

## 1. 校正の方法

成果物のハッシュが同じでも評価は決定的なので、**期待を実行前に宣言する**。
各ケースについて次を固定し、実結果と完全一致で突き合わせる。

| 宣言する項目 | 内容 |
| --- | --- |
| 期待する判定 | `pass` / `fail` / `fail_critical` / `blocked` / `error` |
| 不合格になる要件の集合 | 要件 ID の集合（完全一致） |
| 未評価になる要件の集合 | 要件 ID の集合（完全一致） |
| 終了コード | `0` / `1` / `2` |
| 品質点の有無 | `null` か否か |

「落ちてはいけない要件」を指定するだけでは足りない。**集合の完全一致**にすることで、
予期しない不合格（誤検出）と予期しない合格（見逃し）の両方を検出する。

`runs/` は `.gitignore` の対象であり、実行のたびに作り直される。
追跡する記録は本ファイルと `calibration-summary.json` だけである。

## 2. ケースと実結果（評価版 `1.1.0`）

実行日: 2026-09-17（`run-calibration.ps1` の実行結果、20 ケースすべて一致）
実行環境: `dotnet --version` = `10.0.300-preview.0.26177.108`（成果物と評価器は `net8.0`）

評価版 `1.0.0` の 17 ケースの記録は §5-17・§4.4 と
[`calibration-summary-1.0.0.json`](calibration-summary-1.0.0.json) に**履歴として残す**。
`calibration-summary.json` は最新の実行の出力であり、過去の結果を上書きしない形で分けている。

| # | ケース | 種別 | 期待判定 | 実判定 | 品質 | 期待の不合格 | 実測の不合格 | 実測の未評価 | 一致 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `cal-ref-001` | 正例 | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 2 | `cal-ref-002` | 正例（反復） | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 3 | `cal-alt-001` | 妥当な別実装 | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 4 | `cal-alt-002` | 妥当な別実装（反復） | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 5 | `neg-remove-count-pre-decrement` | 重要な負例 | `fail` | `fail` | 96.55 | `R-014` | `R-014` | （なし） | はい |
| 6 | `neg-no-quantity-multiply` | 重要な負例 | `fail_critical` | `fail_critical` | 93.10 | `R-013` `R-016` | `R-013` `R-016` | （なし） | はい |
| 7 | `neg-duplicate-cart-lines` | 重要な負例 | `fail_critical` | `fail_critical` | 82.76 | `R-012` `R-013` `R-014` `R-015` `R-016` | 同左 | （なし） | はい |
| 8 | `neg-no-seed` | 重要な負例 | `fail_critical` | `fail_critical` | 48.28 | `R-002` `R-003` `R-004` `R-005` `R-006` `R-008` `R-010` `R-011` `R-012` `R-013` `R-014` `R-016` `R-017` | 同左 | `R-023` `R-024` | はい |
| 9 | `neg-destructive-seed` | 重要な負例 | `fail_critical` | `fail_critical` | 96.55 | `R-005` | `R-005` | （なし） | はい |
| 10 | `neg-unknown-album-500` | 重要な負例 | `fail` | `fail` | 96.55 | `R-009` | `R-009` | （なし） | はい |
| 11 | `neg-legacy-wrapper` | 重要な負例 | `fail` | `fail` | 96.55 | `R-029` | `R-029` | （なし） | はい |
| 12 | `neg-compile-error` | 重要な負例 | `fail_critical` | `fail_critical` | 10.34 | `R-001` | `R-001` | `R-002`〜`R-025` `R-028` | はい |
| 13 | `neg-wipe-orders-only` | 重要な負例（**修正後の再確認**） | `fail_critical` | `fail_critical` | 96.55 | `R-005` | `R-005` | （なし） | はい |
| 14 | `neg-whitespace-wrong-quantity` | 重要な負例 | `fail_critical` | `fail_critical` | 89.66 | `R-012` `R-014` `R-016` | `R-012` `R-014` `R-016` | （なし） | はい |
| 15 | `var-single-quoted-attributes` | 妥当な別表現 | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 16 | `var-legacy-name-kept` | 妥当な別表現 | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 17 | `var-whitespace-notation` | 妥当な別表現 | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 18 | `var-legacy-comment-mention` | 妥当な別表現 | `pass` | `pass` | 100.00 | （なし） | （なし） | （なし） | はい |
| 19 | `fault-missing-artifact` | 評価側の障害 | `error` | `error` | `null` | （なし） | （なし） | （なし） | はい |
| 20 | `fault-unimplemented-check` | 評価側の障害 | `error` | `error` | `null` | （なし） | （なし） | （なし） | はい |

`neg-compile-error` の未評価 25 件は `R-002`〜`R-025` と `R-028` である。
起動できないために HTTP で観測できない要件が未評価になり、ビルドを見る `R-001` だけが不合格、
成果物を静的に読む `R-026` `R-027` `R-029` は合格のまま残る。
**成果物のビルド失敗を評価側の障害（`error`）にしない**ことをこの集合で固定している（§4.4）。

`neg-wipe-orders-only` は評価版 `1.0.0` では**期待が `pass`** だった（§5-17）。
`1.1.0` では保存契約（[品質評価仕様 §4.5.1](../../docs/quality-spec.md)）の `Orders` 表を
再起動の前後で観測するため、**期待が `fail_critical`** になり、実結果も一致した。

評価 ID（`calibration-summary.json` に記録）:

| ケース | 評価 ID | 成果物ハッシュ先頭 12 桁 |
| --- | --- | --- |
| `cal-ref-001` | `MS1-001-9a6de335c982-1.1.0-001` | `9a6de335c982` |
| `cal-ref-002` | `MS1-001-9a6de335c982-1.1.0-002` | `9a6de335c982` |
| `cal-alt-001` | `MS1-001-a4a3f8acf2ad-1.1.0-003` | `a4a3f8acf2ad` |
| `cal-alt-002` | `MS1-001-a4a3f8acf2ad-1.1.0-004` | `a4a3f8acf2ad` |
| `neg-remove-count-pre-decrement` | `MS1-001-5dc3f2a5ad81-1.1.0-010` | `5dc3f2a5ad81` |
| `neg-no-quantity-multiply` | `MS1-001-eca552dfe0d1-1.1.0-011` | `eca552dfe0d1` |
| `neg-duplicate-cart-lines` | `MS1-001-68bd495e68c4-1.1.0-012` | `68bd495e68c4` |
| `neg-no-seed` | `MS1-001-623bee9b1abd-1.1.0-013` | `623bee9b1abd` |
| `neg-destructive-seed` | `MS1-001-5f65778d7b2a-1.1.0-014` | `5f65778d7b2a` |
| `neg-unknown-album-500` | `MS1-001-90da6f047c55-1.1.0-015` | `90da6f047c55` |
| `neg-legacy-wrapper` | `MS1-001-5adff2f6252a-1.1.0-016` | `5adff2f6252a` |
| `neg-compile-error` | `MS1-001-d948ae6ed4a2-1.1.0-017` | `d948ae6ed4a2` |
| `neg-wipe-orders-only` | `MS1-001-9cfe8a19e52a-1.1.0-018` | `9cfe8a19e52a` |
| `neg-whitespace-wrong-quantity` | `MS1-001-e2e621e63c2e-1.1.0-019` | `e2e621e63c2e` |
| `var-single-quoted-attributes` | `MS1-001-9f5c9549a476-1.1.0-020` | `9f5c9549a476` |
| `var-legacy-name-kept` | `MS1-001-021e8144df51-1.1.0-021` | `021e8144df51` |
| `var-whitespace-notation` | `MS1-001-d516270ba3c8-1.1.0-022` | `d516270ba3c8` |
| `var-legacy-comment-mention` | `MS1-001-a8e65f51027f-1.1.0-023` | `a8e65f51027f` |
| `fault-missing-artifact` | `MS1-001-000000000000-1.1.0-090` | （成果物なし） |
| `fault-unimplemented-check` | `MS1-001-9a6de335c982-1.1.0-091` | `9a6de335c982` |

既存 13 ケースの**成果物ハッシュは `1.0.0` の記録と一致した**（fixture を変えていない）。
変わったのは評価版の部分だけであり、評価 ID は別の履歴として残る（仕様 §6）。

反復の確認: `cal-ref-001` と `cal-ref-002`、`cal-alt-001` と `cal-alt-002` は
**同じ成果物ハッシュで同じ判定・同じ品質点**を返した（仕様 §6）。

再採点と評価履歴の確認（校正の 20 ケースとは別に個別に実行）:

| 実行 | 成果物 | 評価版 | 連番 | 評価 ID |
| --- | --- | --- | --- | --- |
| 再採点 | `inner/fixtures/reference` | `1.0.0` | 1 | `MS1-001-9a6de335c982-1.0.0-001` |
| 再採点 | 同じ（ハッシュ `9a6de335c982`） | `1.0.0` | 2 | `MS1-001-9a6de335c982-1.0.0-002` |
| 評価版変更 | 同じ（ハッシュ `9a6de335c982`） | `1.0.1` | 7 | `MS1-001-9a6de335c982-1.0.1-007` |
| 評価版変更 | 同じ（ハッシュ `9a6de335c982`） | `1.1.0` | 1 | `MS1-001-9a6de335c982-1.1.0-001` |

- 評価 ID は `{課題ID}-{成果物ハッシュ先頭12桁}-{評価版}-{連番}` であり、
  **成果物が同じでも評価版または連番が変われば別の ID になる**。
- 評価器は `--out` の下だけに書き、他の評価の出力を書き換えない。
  評価履歴を残すかどうかは呼び出し側（校正スクリプト、外側の実行基盤）の責務である。
  **評価器自身は履歴を蓄積しない。** この点を「履歴が自動的に残る」と表現しない。
- 評価版 `1.0.1` の実行後も、`1.0.0` の記録（`runs/cal-ref-001/`）は変更されずに残っていた。

## 2.1 成果物ハッシュの扱い

成果物ハッシュは**成果物ディレクトリ配下の全ファイル**を対象にする（`bin` `obj` `.git` を除く）。
したがって、成果物ディレクトリの中に研究用の文書やメモを置くと、その編集でハッシュが変わってしまう。
実際、正例の説明文書を `inner/fixtures/reference/` の中に置いていたため、その文書の編集で
同じ成果物のハッシュが変わった。現在は説明文書を [`inner/fixtures/README.md`](../fixtures/README.md) に移し、
`reference/` `alternative/` の直下には成果物そのものだけを置いている。

**改行コードもハッシュに効く。** `.gitattributes` は成果物のソースを LF に固定しているが、
作業ツリーのファイルが CRLF のまま残っていると、記録したハッシュは新規 clone で再現しない。
本ファイルの表のハッシュは、作業ツリーを `git checkout` が書き出す改行コードに揃えたうえで記録した。
**ハッシュは「どのバイト列を評価したか」の識別子であり、「同じ実装かどうか」の判定ではない。**

## 3. 4 分類の対応

| 仕様 §7.1 の分類 | ケース | 何を確認するか |
| --- | --- | --- |
| 正例 | `cal-ref-001` `cal-ref-002` | 対象要件を満たす成果物を落とさない。反復で判定が変わらない |
| 実行可能な負例 | `neg-*` の 10 種 | 内容の取り違え・欠落・二重処理を実際に不合格にする |
| 妥当な別実装 | `cal-alt-001` `cal-alt-002` `var-*` の 4 種 | 構造・表現の違いで不当に落とさない |
| 評価側の障害 | `fault-*` の 2 種 | 起動・データ準備・検査の障害を実装品質の失敗と取り違えない |

`var-*` は `inner/fixtures/variants/apply.ps1` が作る「正例 + 等価な別表現」であり、
負例と同じく期待は実行前に宣言する。**期待する判定はすべて `pass`** である。

### 3.1 負例が何を壊すか

| 負例 | 壊す内容 | 期待どおり落ちた要件 |
| --- | --- | --- |
| `remove-count-pre-decrement` | 削除応答の数量を減算前に読む | `R-014` |
| `no-quantity-multiply` | かご合計で数量を掛けない | `R-013` `R-016` |
| `duplicate-cart-lines` | 同じアルバムでも明細を増やす | `R-012` `R-013` `R-014` `R-015` `R-016` |
| `no-seed` | 初期カタログを投入しない | `R-002` `R-003` `R-004` `R-005` `R-006` `R-008` `R-010` `R-011` `R-012` `R-013` `R-014` `R-016` `R-017`（+ 未評価 `R-023` `R-024`） |
| `destructive-seed` | 起動のたびに DB を作り直す | `R-005` |
| `unknown-album-500` | 存在しないアルバムを 404 にしない | `R-009` |
| `legacy-wrapper` | 旧実装を起動する記述を残す | `R-029` |
| `compile-error` | 成果物に構文エラーを入れる | `R-001`（他は未評価。評価側の障害にはしない） |
| `wipe-orders-only` | 再起動のたびに注文を消す（採番だけ継続） | `R-005`（評価版 `1.0.0` では**なし。見逃した**。§4.5） |
| `whitespace-wrong-quantity` | 属性表記の空白を等価にしたうえで数量を誤らせる | `R-012` `R-014` `R-016` |

負例は [`inner/fixtures/negatives/apply.ps1`](../fixtures/negatives/apply.ps1) が正例のコピーに
1 箇所ずつ改変を加えて作る。改変のアンカーが一意に存在することを適用時に検査する。

`no-seed` は**検出したい欠陥が連鎖して落ちる**例である。カタログが無いと、
カタログを前提とする要件がまとめて落ちる。一方、404 系（`R-007` `R-009`）と
注文系（`R-018`〜`R-025`）は落ちない。この「落ちないこと」も集合の完全一致で固定している。

`legacy-comment-only` は評価版 `1.0.0` では負例として扱っていたが、
**コメント中の旧名称は実依存ではない**ため `var-legacy-comment-mention`（妥当な別表現、期待 `pass`）へ移した（§4.5）。

`whitespace-wrong-quantity` は `var-whitespace-notation` と**同じ表記**のまま数量だけを誤らせる。
表記差を許容することと、値の誤りを見逃すことを分けて確認するために対で置いている（§4.5）。

### 3.2 評価側の障害が何を確認するか

| 障害 | 与える入力 | 期待 |
| --- | --- | --- |
| 成果物なし | 存在しないディレクトリを `--artifact` に渡す | 終了コード `2`、`verdict = error`、`quality = null`。`evaluation.json` は書かれる |
| 台帳に実装のない検査 ID | `C-999` を足した台帳のコピーを `--spec` に渡す | 終了コード `2`、`verdict = error`、`quality = null`。採点しない |

## 4. 校正で見つけて直した誤り

校正は評価器と fixture の欠陥を実際に検出した。**検出した内容を隠さず残す。**

| # | 事象 | 原因 | 対応 |
| --- | --- | --- | --- |
| 1 | `neg-remove-count-pre-decrement` が**通ってしまった**（見逃し） | `apply.ps1` の改変アンカーが 1 行だけで、`if (item.Count > 1)` の内側に一致していた。挿入した代入が直後の行で上書きされ、改変が無効になっていた | アンカーを 2 行にし、対象の代入順序を実際に入れ替えるようにした |
| 2 | `neg-no-seed` が `R-023` `R-024` を**誤って不合格にした**（誤検出） | カタログが無いと かごに明細を入れられない。入力検証の検査は前提不成立なのに `fail` を返していた | 前提不成立を `blocked` として返すようにした（`C-024` `C-025`）。期待も `blocked` として宣言 |
| 3 | `fault-unimplemented-check` が `evaluation.json` を**書かなかった** | 明示した `--spec` のディレクトリに `catalog.json` が無く、台帳の読み込み前に終了していた | 台帳・カタログの読み込み失敗と成果物ディレクトリの不在を評価側の障害として集約し、必ず `evaluation.json` を書くようにした。あわせて `--catalog` を明示 |
| 4 | 校正スクリプトが負例の生成失敗を**見逃しえた** | 子スクリプトの `$LASTEXITCODE` に依存していた | 生成後に成果物ディレクトリの存在を確認するようにした |
| 5 | 同じ成果物の**ハッシュが変わった** | 成果物ディレクトリの中に研究用の説明文書を置いていたため、その文書の編集がハッシュに影響していた | 説明文書を [`inner/fixtures/README.md`](../fixtures/README.md) に移した（§2.1） |
| 6 | 記録したハッシュが**新規 clone で再現しなかった** | `.gitattributes` は成果物のソースを LF に固定しているが、作業ツリーのファイルが CRLF のまま残っていた。成果物ハッシュはバイト列を見るため、git が書き出す内容と一致していなかった | 作業ツリーを `git checkout` が書き出す改行コードに揃え、`apply.ps1` の差分アンカーも LF に合わせた（§2.1） |

事象 1・2 は「期待を実行前に宣言して完全一致で突き合わせる」方式にした理由そのものである。
`pass` になるはずのないケースが `pass` になっても、宣言した集合と一致しなければ校正は失敗する。

事象 1 に関連する仕様側の修正（`R-005` の観測点が実際には画面に出ないことを受けての修正）は
本表とは別のコミット `b41f4fb` に記録がある。

### 4.1 評価器の証跡形式を変えた後の再実行

校正の外で 1 件の観測があった。**評価器が強制終了されると、起動したアプリのプロセスが残る**
（実際に 1 つ残っていた）。評価器の `Stop()` はプロセスツリーごと止めるが、
評価器自身が道連れにされると `Stop()` に到達しない。残骸を後から特定できるよう、
`evidence/app-process.log` の先頭行と `evaluator-manifest.json` にアプリのプロセス ID を
残すようにした（[`docs/evaluator.md`](../../docs/evaluator.md) §6.1）。

これは**証跡の形式の変更であり、判定の意味を変えない**。確認のため校正を再実行し、
13 ケースすべてが**同じ判定・同じ品質点・同じ評価 ID・同じ不合格の集合**で一致した。
そのときの [`calibration-summary.json`](calibration-summary.json) は変更前と
**改行コードを除いて完全一致**した（本ファイルの表の値もそのままである）。
このファイルはのちに §4.2 の再実行の出力へ入れ替えてある。

このとき、**評価版は `1.0.0` のままである**。判定の意味は変わっていないためである。
ただし**評価器のビルドは変わっている**ので、`evaluatorSha256` は変更前と異なる。
判定の意味（`evaluation_version`）とビルドの同一性（`evaluatorSha256`）を分けて扱う規則は
[`docs/decision-log.md`](../../docs/decision-log.md) D-15 と
[`docs/outer-harness.md`](../../docs/outer-harness.md) §6.4 にある。
証跡形式の変更後のビルドを測った値は `76ae726991b44b7bbf3c07bbd6416dd4988f1f614ab912e0a227a307e6305e24`
（測ったのは `45a2b4a` の作業ツリー、アセンブリは `bin/Release/net8.0/MusicStore.Evaluator.dll`、
評価器ソースの最終更新は `6abe36e`）。
ただし §4.2 のとおり、**この値は「そのソースをその場所でその作業ツリーのままビルドしたもの」の
識別子**であり、`6abe36e` のビルドを再現して測った値として扱わない（現在の値は `210205b9…`。§4.3）。
**値の正は [`outer/verify/verification-summary.json`](../../outer/verify/verification-summary.json) の
`artifacts.evaluator_sha256` であり、本文の値はそれを写したものである。**
評価器のソースを変えるとこの値は陳腐化するため、本文の値だけを根拠にしない。

この値の再現性は 2 通り測った。**同じソースを同じパスで作り直すと同じ値になる**
（`bin` と `obj` を消して 2 回ビルドし、2 回とも同じ値）。**ソースを別のパスに置いてビルドすると値が変わる**
（`%TEMP%` に同じ `.cs` と `.csproj` を置いてビルドすると `9f1bbdf5f4cdade2…` になった）。
つまりこの値は「そのソース」ではなく「**そのソースをその場所でビルドしたもの**」の識別子である。
**ソースファイルのバイト列（改行コード）も値に効く**（§4.2 の実測）。
固定値を使う場合は条件の `evaluation.evaluator_sha256` に入れるが、そのときは
**`evaluation.evaluator_build` にソースの位置・アセンブリ・ビルドコマンド・ビルドに使った SDK の版・
固定値の出所・クリーンな作業ツリーで測ったこと（`clean_worktree`）も書く**（[`docs/outer-harness.md`](../../docs/outer-harness.md) §6.4）。
**別のマシンや別のパスでの一致は期待しない**（§5-13）。

### 4.2 ビルドの同一性からコミットを外した後の再実行

`evaluatorSha256` は「ソースの版」ではなく「ビルドしたもの」の識別子だと整理したが、
その値が**ビルドしたコミットにも依存していた**。.NET SDK は既定で、ビルドした作業ツリーの
コミットを `AssemblyInformationalVersion` に埋め込むためである。実測で確認した。

| ビルドした時点の `HEAD` | 測った `sha256` | 注 |
| --- | --- | --- |
| `45a2b4a` | `76ae726991b44b7b…` | コミットの文字列はアセンブリに無い |
| `6675e19`（文書だけのコミット） | `60c8fbd406ad6484…` | アセンブリに `6675e19` の文字列がある |

アセンブリの中に `6675e19` の文字列があることも確認した（`45a2b4a` のビルドには無い）。
**この 2 つの絶対値は、後述のとおりバイト列を作業ツリーから採ったものなので再現値として扱わない**が、
**文書だけのコミットで値が変わったこと**と**コミットの文字列が埋まっていたこと**は、
埋め込みの事実を示す観測である。この状態では、**測定値を記録するコミット自身が次のビルドの値を
変えてしまう**ため、記録した値とその記録を含むコミットが一致しえない。そこで評価器の `.csproj` で
`IncludeSourceRevisionInInformationalVersion` を `false` にし、**値がソース・パス・SDK だけ**で
決まるようにした（評価器はこの属性を読まないため、判定の意味は変わらない）。
生成される `AssemblyInformationalVersion` が `1.0.0` になっていること、アセンブリにコミットの文字列が
無いこと、そして `-p:SourceRevisionId=<別の値>` を与えても値が変わらないことを確認した。

校正を再実行し、13 ケースすべてが**同じ判定・同じ品質点・同じ評価 ID・同じ不合格の集合**で
一致した。[`calibration-summary.json`](calibration-summary.json) はこの実行の出力に入れ替えてある。
入れ替えで前の記録と差が出たのは**評価側の障害 2 件の `stderr` の書き方だけ**である。
原因は**実行した PowerShell のホスト**で、Windows PowerShell 5.1 はネイティブコマンドの標準エラーを
`dotnet.exe : …` と `At <行>` の形に包んで記録するのに対し、PowerShell 7 は包まない
（前の記録は 5.1 で 6 行、今の記録は 7 で 1 行。判定・品質点・終了コードは同じ。§5-15）。
この差は評価器の出力ではなく、実行のしかたに依存する。
**この記録を `pwsh` 7 で作り直すと、[`calibration-summary.json`](calibration-summary.json) と
行末以外まったく同じバイト列になった**（記録に評価器のビルドのハッシュは入らないため、
この一致は「判定の意味が変わっていない」ことの確認であり、ビルドが同じことの確認ではない。§4.3）。

この変更でも**評価版は `1.0.0` のままである**。判定の意味は変えていない。

**このとき本文に書いた値 `09679d524befd9cb…` は、その後どこからも再現しなかった。**
クリーンな作業ツリーで測り直すと `d6798301a91d97a5…` になり、`bin` `obj` を消した作り直しでも
同じ値になる（4 回）。埋め込みを切った後も値が変わっていたのは、
**`09679d52…` を測った作業ツリーのソースのバイト列がコミットと一致していなかった**ためである。
`.cs` を CRLF で保存してビルドすると値が変わることが実測で確認できた
（LF は `d6798301a91d97a5…`、CRLF は `d5443910a342d9f6…`）。
`.csproj` のバイト列は値に効かない（CRLF にしても `d6798301…` のまま）。
追跡ファイルを書き換えていないクリーンな作業ツリー（`.gitattributes` の `eol=lf` のまま）で測る、
という条件が抜けていた**記録のしかたの誤り**である。§4.3 で手順に入れた。

**さらに、`d6798301…` もコミットをまたぐと変わった。** 記録を含むコミットを作った直後の
ビルドは `3a5040c5ec6de531…` になり、値はまだコミットに依存していた。
アセンブリの中にコミットの文字列は無いのに値が変わるため、経路を分けて確かめた。

| 確かめたこと | 結果 |
| --- | --- |
| `-p:EnableSourceLink=false`（sourcelink 文書を作らない） | コミットを変えても値が変わらない（同じ値） |
| `-p:EnableSourceLink=true`（既定） | 同じソースでも値が変わる |
| 生成される `obj/…/MusicStore.Evaluator.sourcelink.json` | 中身が `…/raw.githubusercontent.com/fukuda-yuki/sample2/<コミット>/*` |
| `-p:DebugType=none`（PDB を作らない） | さらに別の値になる |

つまりコミットは**2 つ目の経路**で入っていた。SDK が作る sourcelink 文書に**コミットそのもの**が
入り、Roslyn がそれを**移植可能 PDB に入れ**、アセンブリはその **PDB の id をデバッグディレクトリに
持つ**。だからコミットの文字列がアセンブリの中に無くてもバイト列が動く。
評価器はこの文書を読まないので、`.csproj` で `EnableSourceLink` も `false` にした。
**判定の意味は変わらないため評価版は `1.0.0` のままである。**
この変更後の値は `06fa548cad3be2c1e2a4bbdd1e97687fb333864661cc4ab478dc3ee673cef388` であった
（§4.3 で記録コミットをまたいだ一致を確認した）。
**その後、レビュー指摘への修正で評価器のソースを変えたため、値は測り直しになった**
（2026-09-16 の 17 ケース時点で `4eeca67b…`、2026-09-17 の 20 ケース時点で `210205b9…`。§4.3）。

ビルドしたコミットは、条件の `evaluation.evaluator_build.source_commit` に追跡用として残す
（値には影響しない）。

### 4.3 記録した値を記録コミットの後で作り直して確かめる

値が「ソースの版」ではなく「ビルドしたもの」の識別子である以上、**測って記録するだけでは足りない**。
記録を書き込むコミットで値が変わらないことを、次のように確かめる手順にした。

1. 追跡ファイルを書き換えていない**クリーンな作業ツリー**で測る（`git status --porcelain` が空）。
2. 外側の検証が評価器を `bin` `obj` を消してからビルドし（`V-0`）、もう一度消して作り直して
   **同じ値になることを `V-0b` で確認する**（採点に使う前。
   [`outer/verify/README.md`](../../outer/verify/README.md) §V-11）。
3. その値を記録に書き、**記録を含むコミットを作る**。
4. **そのコミットの後でもう一度作り直し、記録した値と同じになることを確かめる。**

2026-09-16 時点の結果:

| 確認 | 結果 |
| --- | --- |
| `V-0b`（`bin` `obj` を消したビルドが 2 回続けて同じ値になる） | 一致 |
| 校正の再実行（`pwsh` 7、13 ケース） | すべて一致。`calibration-summary.json` と**行末以外は同じバイト列**（記録にビルドのハッシュは入らない） |
| 記録コミット（`786bcfe`）の後でのビルド | `06fa548cad3be2c1…`（記録した値と一致） |

レビュー指摘への修正（2026-09-16、17 ケース）でも同じ手順を踏んだ。
**この回は評価器のソースを変えたため、値は測り直しになった**（`4eeca67b…`）。

| 確認 | 結果 |
| --- | --- |
| `V-0b`（`bin` `obj` を消したビルドが 2 回続けて同じ値になる） | 一致 |
| 校正の再実行（`pwsh` 7、17 ケース） | すべて一致（[`calibration-summary-1.0.0.json`](calibration-summary-1.0.0.json)） |
| 記録コミットの後でのビルド | 記録を作ったコミットの後で作り直して確かめる（§4.3 の手順 4） |

再レビュー指摘への修正（2026-09-17、20 ケース）でも同じ手順を踏んだ。
**この回も評価器のソースを変えたため、値は測り直しである**（`210205b9…`）。
この回は**判定の意味も変わる**（`R-005` の保存契約。D-24）ため、評価版も `1.0.0` → `1.1.0` に上げた（D-25）。

| 確認 | 結果 |
| --- | --- |
| `V-0b`（`bin` `obj` を消したビルドが 2 回続けて同じ値になる） | 一致（`210205b9fe6aae35…`） |
| 校正の再実行（`pwsh` 7、20 ケース） | すべて一致（[`calibration-summary.json`](calibration-summary.json)） |
| `outer/verify/verify.py`（18 ケース） | 不一致 0 |
| 記録コミットの後でのビルド | 記録を作ったコミットの後で作り直して確かめる（§4.3 の手順 4。**本記録の時点では未実施**） |

記録コミットの後で値が変わらないことは、**コミットを値に混ぜなくした効果そのもの**の確認でもある。
埋め込みが有効なビルドでは `AssemblyInformationalVersion` が `1.0.0+<コミット>` になり、
sourcelink が有効なビルドでは値がコミットをまたいで変わる
（実測: `-p:EnableSourceLink=true` は別の値、`false` は同じ値。§4.2）。
この手順を踏まずに固定すると、条件に**再現しない値**を入れて、
すべての採点が `rejected_mismatch` になる（§5-16）。

上の表の「記録コミット」は**記録を書いたコミット**であり、評価器のソースを最後に変えたコミットとは
限らない。値の正は
[`outer/verify/verification-summary.json`](../../outer/verify/verification-summary.json) の
`artifacts.evaluator_sha256` であり、測ったのは
`inner/evaluator/MusicStore.Evaluator/bin/Release/net8.0/MusicStore.Evaluator.dll`、
ビルドは `dotnet build inner/evaluator/MusicStore.Evaluator/MusicStore.Evaluator.csproj -c Release` である。
固定値を条件に入れるときは、これに測った前提（`clean_worktree`）を加えた 5 つ
（値の出所・ソースの位置・コマンド・ビルドに使った SDK・クリーンな作業ツリーで測ったこと）を
`evaluation.evaluator_build` に書く。`source_path` と `command` があっても、その作業ツリーの
ソースのバイト列（改行コード）が記録と違えば値は再現しないため、前提そのものを必須にしている
（[`docs/outer-harness.md`](../../docs/outer-harness.md) §6.4）。

### 4.4 外部レビューの再現ケースを校正へ入れた回

外部レビューが「誤合格・誤不合格を再現した」として 8 件を挙げた。**すべて本リポジトリの
コードで再現し、再現ケースを校正の宣言済みケースとして追加した。** 修正の要点だけを残す。

| レビューの指摘 | 再現したこと | 対応 | 固定したケース |
| --- | --- | --- | --- |
| 静的検査が発行の後に走る | 成果物に構文エラーを入れると静的検査が例外を出し、**評価側の障害（`error` / `quality = null`）**として報告された。実装の失敗と評価器の障害が区別できない | `RunStatic` を発行の前に移し、静的前提が満たせない検査は `blocked`、評価器自身の障害は `error` として返す | `neg-compile-error` |
| 上記の修正中に見つかった潜在不具合 | `StaticResult.DatabaseFileExists` が `RunStatic` の時点の**スナップショット**だったため、静的検査を前に移した後は DB ファイルがまだ無く、`C-029` が**すべての成果物で**不合格になった（正例が 96.55 点になった） | 実ファイルを見る計算プロパティにした | 正例 2 件・別実装 2 件が 100.00 に戻ることで検出 |
| 引用符の流儀を不合格にする | 属性を単一引用符に変えるだけで **100.00 → 62.07**（`R-012`〜`R-016`） | HTML を読む正規表現を `"` と `'` の両方に一致させた | `var-single-quoted-attributes` |
| 旧名称を旧実装への依存とみなす | 名前空間とアセンブリ名を `MvcMusicStore` にしただけの正常な実装が **96.55 点・不合格**（`R-029`） | 旧実装への依存は**旧実装の成果物・旧ホスト・旧フレームワーク**の信号で判定し、名前空間の語だけでは判定しない | `var-legacy-name-kept` |
| 注文データの消失を見逃す | 再起動のたびに注文を全削除する実装が **29/29・100.00 で合格**した | **この回では直していない。** 検出には「買い手の識別を再起動をまたいで維持する」契約の追加が必要で、要求そのものを変える判断になるため今回は行わない（§5-17）。**評価版 `1.1.0` で修正した（§4.5）** | `neg-wipe-orders-only`（この回の**期待は `pass`**） |
| 固定後の成果物の変更を受け入れる | `frozen/` を書き換えても回収時のハッシュと照合せず再採点していた | 回収時ハッシュと一致しなければ採点せず `rejected_mismatch` を記録する | `outer/tests/test_evaluate.py` |
| 再採点で前回の DB を引き継ぐ | 2 回の採点で注文が **3 件 → 6 件**になり、「毎回空の DB」という初期条件を満たさなかった | 実行の連番ごとに作業ディレクトリを分け、評価器は作業ディレクトリが空でなければ**評価側の障害**として採点しない | `outer/tests/test_evaluate.py`、`outer/tests/stub_evaluator.py` |
| 別の実行の使用量を受け入れる | 実行 A の 125 tokens を実行 B の完全な使用量として記録できた | 使用量のイベントを実行 ID で束縛し、一致しないイベントは記録しない | `outer/tests/test_usage.py`、`outer/tests/test_harness.py` |
| 伝えていない要件で採点する | 公開していない契約（接続文字列の設定方法、未認証購入の扱いなど）を要求していた | **観測できる契約を `condition.json` の `migration_request` に公開**した。要件と検査の対応は変えていない | `outer/conditions/MS1-001/condition.json` |
| 発行が成果物ディレクトリを書き換える | 正例の採点で、評価器の `dotnet publish` が `frozen/` の中に `obj/` 45 件と `bin/` 39 件を書いた。`bin` `obj` は成果物ハッシュの除外規則で無視されるため、**ハッシュは変わらない**（採点した成果物と固定した成果物が別物でも気づけない） | 成果物を作業ディレクトリへ複製し、**複製に対して発行する** | `outer/verify/verify.py` の `V-14` |

最後の 1 件は校正のケースではなく**入力の公開**である。採点の基準（`inner/` の要件・検査）は
動かさず、実装役に渡す文書に**観測できる形の契約**を書いた。§5-18 に残る限界も併せて読むこと。

**最後の 2 件（発行の書き換え）は校正では検出できなかった。** 校正の負例は成果物を書き換える
欠陥を含まないため、**採点が成果物を壊していても校正はすべて一致する**。
検出したのは外側の検証（`V-14`）であり、校正のケースを増やしても見つからない種類の欠陥である。

この回でも**評価版は `1.0.0` のままである**。理由は §4.1・§4.2 と同じで、
**判定の意味を変える修正ではなく、仕様と台帳が既に定めていた意味に実装を合わせる修正**だからである
（[`docs/decision-log.md`](../../docs/decision-log.md) D-16）。この判断は次の確認で支えている。

| 確認 | 結果 |
| --- | --- |
| 既存 13 ケースの判定・品質点・評価 ID・不合格の集合 | 変更前と一致（`calibration-summary.json` の 1〜11・16・17 行） |
| 既存 13 ケースの成果物ハッシュ | 変更前と一致（fixture を変えていない） |
| 新しい 4 ケース（`neg-compile-error` `neg-wipe-orders-only` `var-*` 2 件） | 宣言した期待と一致 |
| 17 ケースすべて（発行を複製に対して行う修正の後） | 宣言した期待と一致（§4.3 の 2 つ目の表） |

この回の `neg-wipe-orders-only = pass` は**未解消の欠陥を期待値として固定した記録**である。
**品質評価の受入成功として数えない。** この記録は履歴として残し、
評価版 `1.1.0` の結果で上書きしない（§4.5、[`calibration-summary-1.0.0.json`](calibration-summary-1.0.0.json)）。

### 4.5 再レビューの指摘を直した回（評価版 `1.1.0`）

再レビューが「重大な見逃し・誤不合格・記録破壊」として 4 件を挙げた。**4 件すべてを修正し、
宣言済みケースを 17 → 20 件に増やして実測した。**

| レビューの指摘 | 再現したこと | 対応 | 固定したケース |
| --- | --- | --- | --- |
| 再起動で注文を全削除する成果物を満点・合格にする（**重大**） | `neg-wipe-orders-only` が **100.00 で合格**。`R-005` の検査は再起動前後の**注文番号が異なること**だけを見ており、再起動前の注文が残っていることを見ていなかった。`R-005` は重大要件なので、単なる評価対象外にはできない | **保存契約**（`Orders` 表の `OrderId` 列）を公開し、`R-005` の検査を「再起動の**前**に注文行が存在し、同じ DB で再起動した**後**も同じ注文行が存在すること」に変えた（読み取り専用・新しい注文を作る**前**に確認）。観測できない場合は合格にしない | `neg-wipe-orders-only`（**期待を `fail_critical` に変更**）、`neg-destructive-seed` |
| 同じ採点連番の再指定を、作業領域とログを書き換えた**後**に拒否する（**重大**） | 連番 1 の採点後、同じ連番 1 で再度呼び出すと、以前の作業領域を削除し、同じログを上書きモードで開き、評価器を再実行してから重複を拒否していた。`record.json` が参照する DB・ログとの対応が壊れる | 使用済み連番の確認を**作業領域の削除・作成、ログのオープン、評価器の起動より前**に移した。作業領域は削除しない。重複の拒否は `evaluations/duplicate-refusals.jsonl` と別ディレクトリの `record.json` に記録し、`index.jsonl` には書かない | `outer/tests/test_evaluate.py`、`outer/verify/verify.py` の `V-4` |
| コメント中の旧フレームワーク名を旧実装への依存として不合格にする | 正常な実装に `// Migrated from net48 to net8.0; no legacy process is used.` を足すだけで `R-029` が不合格になった | コメント・説明文を**除去してから**旧実装の参照・起動の信号を探すようにした。文字列リテラルは残すため、実際に旧実装を起動する記述は引き続き検出する。旧名称の**言及**は診断情報として記録し、判定には使わない | `var-legacy-comment-mention`（**妥当な別表現として追加**）、`neg-legacy-wrapper`、`var-legacy-name-kept` |
| HTML 属性の `=` 前後の空白で正常な成果物を不合格にする | `id="cart-total"` を `id = "cart-total"` と書くだけで明細行・数量・合計が取得できず、`R-012`〜`R-016` が落ちた | 属性の解析を `=` 前後の空白・タブ・改行に依存しないようにした。二重引用符・単一引用符の両方を維持し、HTML 全体の空白を削除する前処理はしない | `var-whitespace-notation`（**追加**）、`neg-whitespace-wrong-quantity`（**追加**）、`var-single-quoted-attributes` |

**この回は評価版を `1.0.0` → `1.1.0` に上げた。** 前回までの回と違い、
`R-005` の**判定の意味そのものが変わる**（同じ成果物の判定が `pass` から `fail_critical` に変わりうる）。
実装を仕様に合わせる修正ではなく**要求と観測方法の追加**であるため、
[`docs/decision-log.md`](../../docs/decision-log.md) D-17 として分けて記録した。

| 確認 | 結果 |
| --- | --- |
| 既存 13 ケースの判定・品質点・不合格の集合 | 変更前と一致（§2 の表） |
| 既存 13 ケースの成果物ハッシュ | `1.0.0` の記録と一致（fixture を変えていない） |
| 20 ケースすべて | 宣言した期待と一致（`run-calibration.ps1` が終了コード `0`） |
| 誤不合格の解消 | `var-whitespace-notation` `var-legacy-comment-mention` が `pass`（100.00） |
| 見逃しの解消 | `neg-wipe-orders-only` が `fail_critical`（`R-005` のみ不合格、96.55） |
| 実依存の検出が残ること | `neg-legacy-wrapper` が `fail`（`R-029`） |
| 値の誤りを見逃さないこと | `neg-whitespace-wrong-quantity` が `fail_critical`（`R-012` `R-014` `R-016`） |
| 他の要件への意図しない判定変化 | なし（既存 13 ケースの不合格の集合が一致） |

実装中に 1 件、**評価器側の不具合を自分で作り込んだ**。注文行を読むために
`Microsoft.Data.Sqlite` を使ったところ、既定の**接続プール**が `Close()` の後もファイルを掴み続け、
`neg-destructive-seed`（起動のたびに DB を作り直す）が
`The process cannot access the file ... because it is being used by another process` で
**`R-001` 不合格・25 件未評価**になった。`Pooling = false` にして解消し、
`R-005` のみ不合格の 96.55 に戻った。**読み取り専用の観測でも、成果物の動作を変えうる**例として残す。

`neg-wipe-orders-only` の**修正前の記録（`pass` / 100.00）は履歴として残す**。
[`calibration-summary-1.0.0.json`](calibration-summary-1.0.0.json) が評価版 `1.0.0` の 17 ケース、
[`calibration-summary.json`](calibration-summary.json) が評価版 `1.1.0` の 20 ケースである。
**修正後の結果で過去の記録を上書きしない。**

## 5. 校正の限界（隠さず記録する）

1. **正例を評価器と同じ作業者が書いている。** `cal-ref-*` が通ったことは評価器の妥当性の根拠にならない。
   正例の実装を「正解の定義」として扱わない。根拠は要件ごとの `basis` と、別実装・負例の校正に置く。
2. **負例は 10 種であり、課題の全誤りを網羅しない。** 校正の通過を
   「すべての入力に対する完全な等価性」や「モダナイズ品質全般の保証」と表現しない。
   うち 1 種（`wipe-orders-only`）は**評価版 `1.0.0` で見逃すことを期待として固定していた**。
   評価版 `1.1.0` では期待を `fail_critical` に変え、実測も一致した（§4.5、§5-17）。
3. **負例は正例の 1 箇所改変である。** 複数の欠陥が同時に存在する場合の判定は確認していない。
4. **旧実装を実行していない。** `legacy` を根拠とする期待値はコード読解に基づく。
   負例が「旧実装と同じ挙動」であることの確認ではない。
5. **独立した別実装は 1 つだけである。** 構造・表現の違いのうち、この 1 つが示す範囲でしか
   「不当に落とさない」ことを確認していない。別実装は 最小 API・素の SQLite・文字列組み立て HTML を使い、
   正例（MVC・EF Core・Razor ビュー）とは実装方式を変えている。
   `var-*` の 4 件は**正例に等価な別表現を機械的に与えたもの**であり、
   実装方式の独立性は持たない（表現の揺れを落とさないことの確認に限る）。
6. **校正が宣言している評価側の障害は 2 種だけである。** 発行の失敗、起動のタイムアウト、ポート衝突などは
   宣言した期待として固定していない。作業ディレクトリが空でない場合の障害は外側の試験
   （`outer/tests/test_evaluate.py`、`outer/tests/stub_evaluator.py`）で確認しており、
   校正のケースにはしていない（§4.4）。
7. **環境依存。** 校正は `dotnet` の版とポートの空き状況に依存する。実行環境の `dotnet --version` は
   本ファイルに記録しているが、`evaluation.json` には記録していない。別環境での再現は確認していない。
8. **測っていない品質がある。** 仕様 §2.1 の項目について、成果物が良いとも悪いとも主張しない。
9. **成果物ハッシュは成果物ディレクトリの置き方に依存する。** 同じ実装でも、成果物の外に置く文書を
   中に移せばハッシュは変わる。ハッシュは「この木を評価した」ことの識別子であり、
   「実装の意味が同じ」ことの証明ではない（§2.1）。
10. **成果物ハッシュは改行コードにも依存する。** `.gitattributes` でソースを LF に固定しているが、
    それに合わない作業ツリーで評価すると、記録したハッシュは新規 clone で再現しない（§2.1、§4-6）。
    本ファイルのハッシュは揃えた状態で記録しているが、別環境での再現は確認していない。
11. **`calibration-summary.json` は絶対パスを含む。** `runs` の位置と成果物のパスが
    `C:\Users\...` として記録されるため、この記録の突き合わせは**同じマシンでしかできない**。
    成果物ハッシュと評価 ID の再現性とは別の話である（前者はパスに依存せず、後者は依存する）。
12. **評価版は判定の意味の版である。** 校正が確認しているのは「この評価版の意味の下で
    期待と一致すること」であり、**評価器のビルドの同一性は確認していない**。
    現在の評価版は `1.1.0` である（`1.0.0` → `1.1.0` の理由は §4.5 と
    [`docs/decision-log.md`](../../docs/decision-log.md) D-17）。
    ビルドの同一性は `evaluator-manifest.json` の `evaluatorSha256` が表す
    （[`docs/evaluator.md`](../../docs/evaluator.md) §6.2、[`docs/outer-harness.md`](../../docs/outer-harness.md) §6.4）。
13. **`evaluatorSha256` はソースの版ではなく、ビルドした場所と SDK を含む。** 同じソースでも置き場所を
    変えると値が変わる（§4.1）。同じソース・同じパスなら作り直しても同じ値になることは
    このマシンで確認したが、**別のマシン・別のパス・別の SDK での一致は確認していない**。
    この値の一致を「同じ意味の判定をする評価器である」ことの根拠に使わない。
14. **ビルドしたコミットは値に混ぜていない**（§4.2）。コミットは 2 つの経路で入っていた。
    `AssemblyInformationalVersion` への埋め込みと、**sourcelink 文書が移植可能 PDB に入り、
    アセンブリがその PDB の id を持つ**経路である。混ぜていた間は文書だけのコミットでも値が
    変わっており、**値を記録するコミット自身が次のビルドの値を変えていた**。
    `.csproj` で両方を切った後も、**この切り方を知らない別のビルド手順では値が変わりうる**
    （手で `AssemblyInformationalVersion` を指定する、`EnableSourceLink` を戻す、別の SDK を使うなど）。
    固定値の出所にコマンドと SDK を書くのはこのためである。
15. **`calibration-summary.json` の「評価側の障害」2 件の `stderr` は、実行する PowerShell の
    ホストに依存する。** Windows PowerShell 5.1 はネイティブコマンドの標準エラーを
    `dotnet.exe : …` と `At <行>` の形に包むため 6 行になり、PowerShell 7 は包まないため 1 行になる
    （§4.2。判定・品質点・終了コードは同じ）。記録は **PowerShell 7** の出力であり、
    実行コマンドは本ファイルの冒頭にある。この 2 フィールドは同一性の比較に使わない。
16. **`evaluatorSha256` は、ソースファイルのバイト列（改行コードを含む）にも依存する。** 同じ `.cs` を
    CRLF で保存してビルドすると値が変わる（実測: LF `d6798301…`、CRLF `d5443910…`。§4.2）。
    したがって記録に使う値は**追跡ファイルを書き換えていないクリーンな作業ツリー**で測り、
    **記録コミットの後で作り直して同じ値になることを確かめる**（§4.3）。
    この手順を踏まずに固定値を条件に入れると、**再現しない値**を入れてしまい、
    すべての採点が `rejected_mismatch` になる（拒否は記録に残るので静かには壊れない）。
    本ファイルの「4.1」「4.2」にある古い値（`76ae7269…`、`60c8fbd4…`、`09679d52…`、
    `d6798301…`、`d5443910…`、`3a5040c5…`）は、この条件やコミットを混ぜない条件を満たす前の
    作業ツリーで測ったものであり、**再現値として扱わない**。現在の値は `210205b9…` である。
    値の正は [`outer/verify/verification-summary.json`](../../outer/verify/verification-summary.json) の
    `artifacts.evaluator_sha256` である。
17. **`R-005`（注文の永続化）の見逃しは評価版 `1.0.0` で解消していなかった。**
    評価版 `1.0.0` の検査は、再起動後に注文を読み出せるかと、注文番号が変わらないかだけを見ていた。
    注文を消しても、SQLite の `OrderId INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT` は
    `sqlite_sequence` に採番を残すため、**消した後の注文にも新しい番号が付き、番号は前後で変わらない**。
    同じ作業ディレクトリで再起動するため、再起動そのものも成功する。
    したがってこの欠陥は**注文番号の変化では検出できず**、
    `neg-wipe-orders-only` は `pass` / 100.00 になっていた。
    この記録は履歴として残す（[`calibration-summary-1.0.0.json`](calibration-summary-1.0.0.json)）。
    **評価版 `1.1.0` で保存契約を公開し、再起動前後の注文行の存在を見るように変えた**（§4.5）。
    現在は `neg-wipe-orders-only` が `fail_critical` / `R-005` になる。
    ただし**確認しているのは「注文行が残ること」だけである**。注文の内容・金額・買い手との対応、
    複数注文の順序は見ていない（仕様 §4.5.1）。
    **`R-005` の合格を「注文が失われない」ことの完全な証明として使わない。**
18. **要件と検査の対応は今回変えていない。** §4.4 の最後の 1 件は、公開していなかった契約を
    `condition.json` の `migration_request` に**書いた**修正であり、採点の基準を動かしていない。
    したがって**この修正より前に評価した成果物の判定は、契約を知らないまま受けたものである**。
    過去の採点記録を「公開された契約に基づく採点」として読み替えない。
    契約の文面と要件の `basis` が食い違っていないことは、要件ごとに人が突き合わせて確認した
    （自動の突き合わせは無い）。
19. **保存契約は評価版 `1.1.0` から適用し、過去の成果物へ遡って適用しない。** 評価版 `1.1.0` は
    注文の表 `Orders` と列 `OrderId` を公開契約として要求する（仕様 §4.5.1）。
    正例と妥当な別実装はどちらもこの形を使っており、**fixture を変えずに観測できた**。
    しかし**この形を満たさない成果物は、注文を保持していても `R-005` の前提が成立せず未評価になる**。
    未評価は合格ではない（[`docs/quality-spec.md`](../../docs/quality-spec.md) §4.5.1 の判定表）。
    表や列が無い場合を「注文が消えた」事実と混同せず、**保存契約違反**として別に記録する。
20. **`neg-compile-error` の「未評価 25 件」は、起動できない成果物に対する集合である。** ビルドが通る
    成果物で起動だけが失敗する場合（依存の欠落、起動時の例外）の集合は固定していない。
    未評価の件数を「実装の欠陥の大きさ」として読まない。
21. **重複した採点連番の拒否は、校正のケースではない。** 外側の実行基盤の挙動であり、
    `outer/tests/test_evaluate.py` と `outer/verify/verify.py` の `V-4` で確認している（§4.5）。
    校正（`run-calibration.ps1`）は評価器だけを対象にし、外側の実行基盤を起動しない。
    **校正が 20 件一致したことを、重複拒否の確認完了として数えない。**
