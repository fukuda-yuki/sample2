"""Non-model tests for the outer harness.

This file exists so that `python -m unittest discover` finds these tests when
it is started from the repository root or from `outer/`. Without it, discovery
from `outer/` silently reports "0 tests", which reads like "there are none".
"""
